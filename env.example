# Turso Database Configuration
# Get these values from Turso after creating your database
# See TURSO_SETUP.md for detailed instructions

TURSO_DATABASE_URL=libsql://your-database-name.turso.io
TURSO_AUTH_TOKEN=your-auth-token-here

# Resend (verification emails)
# Get your API key from https://resend.com → API Keys
RESEND_API_KEY=re_your_api_key_here
# Optional: custom sender (must be a verified domain in Resend). If unset, uses onboarding@resend.dev for testing.
# RESEND_FROM_EMAIL=Lyven <noreply@yourdomain.com>

# Supabase (backend – e.g. for Rork / buyer testing)
# Get these from your Supabase project: Settings → API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
# Service role key: server-side only, never expose to frontend
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
# Optional: use Supabase PostgreSQL as the backend database (instead of Turso)
# Get from Supabase Dashboard → Settings → Database → Connection string (URI)
# Format: postgresql://postgres.[ref]:[YOUR-DB-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres
# SUPABASE_DATABASE_URL=postgresql://postgres.xxxxx:password@aws-0-eu-west-1.pooler.supabase.com:6543/postgres
